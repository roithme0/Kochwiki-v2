interface:
  display_name: "Review Delivery"
  short_description: "Review a completed spec delivery"
  default_prompt: "Use $review-delivery to review this completed delivery against its spec and the broader codebase."

policy:
  allow_implicit_invocation: false
